/*
    Reduce
    Ele não retorna outro array, ele vai executar uma função em todos os
    elementos do array e retornar um valor único
*/

// Sintaxe

const vetor = [1, 2, 3, 4]

vetor.reduce(callbackFn, initialValue)

/* 
    callbackFn
    O callbackFn é uma função a ser executada em cada elemento do vetor.

    initialValue(opcional)
    É o valor que o retorno final vai atuar
*/
